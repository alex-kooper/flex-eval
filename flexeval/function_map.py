from operator import add, sub, mul, truediv, neg, pos
from .built_in_functions import *

FUNCTIONS = {
    ADD: add,
    SUBTRACT: sub,
    MULTIPLY: mul,
    DIVIDE: truediv,
    NEGATE: neg,
    POSITIVE: pos
}
